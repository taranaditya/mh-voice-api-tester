# Set this only in your local .env file. Never commit the real private API address.
VOICE_API_URL=<PRIVATE_VOICE_API_ENDPOINT>

# Local-only tester settings.
VOICE_API_TIMEOUT_SECONDS=45
TESTER_DATA_DIR=./data
